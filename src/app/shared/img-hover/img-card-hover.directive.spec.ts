import { ImgCardHoverDirective } from './img-card-hover.directive';

describe('ImgHoverDirective', () => {
   it('should create an instance', () => {
      const directive = new ImgCardHoverDirective();
      expect(directive).toBeTruthy();
   });
});
